# Web框架
fastapi==0.110.0
uvicorn[standard]==0.27.0
python-multipart==0.0.9

# HTTP客户端
httpx==0.27.0

# 数据处理
pydantic==2.6.0

# 串口通信
pyserial==3.5

# IoTDB
apache-iotdb==1.3.0
numpy<2.0   # pandas 1.5.3 兼容性要求 (apache-iotdb 1.3.0 的传递依赖)

# MySQL 数据库（版本受 apache-iotdb 1.3.0 约束: >=1.4,<1.5）
sqlalchemy>=1.4,<1.5
pymysql==1.1.0

# JWT & 密码哈希
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# dai: passlib 1.7.4 still reads bcrypt.__about__, removed in bcrypt 4.1+.
bcrypt==4.0.1

# 系统监控
psutil==5.9.8

# Redis 缓存
redis[hiredis]==5.0.1

# 工具
python-dotenv==1.0.0
loguru==0.7.2
minio>=7.2.0

# YOLO 目标检测
ultralytics>=8.0.0
# dai: OpenCV 4.12+ requires NumPy 2, while IoTDB/pandas pins NumPy <2.
# Pinning below 4.12 keeps one compatible cv2 distribution in the image.
opencv-python>=4.8.0,<4.12
